# ECE60131 Final Project: Federated Learning for Selective State-Space Models

## Environment Setup (Conda)
To execute the code, first prepare the environment. 

### 0. Install Anaconda (if not already installed)
### 1. Create a new conda environment
```bash
conda env create -f ai_default.yml
```
### 2. Activate the environment
```bash
conda activate ai_default
```

## Executing the Code
The project code is organized into two main directories:
- `aggregation_experiment`: 